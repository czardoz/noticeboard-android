This is a sample android application that acts like a notice board.
It uses an XML file on a remote server to download and view notices. 
It uses the universal image loader library to download images asynchronously.
